/*
** view buffer generator Flat Tics -- generator for tpyed arrays for Tics geometry for Flat scene
** Copyright (C) 2025-2025 Tactile Interactive, all rights reserved
*/

#include "../hilbert/qSpace.h"
#include "qAvatar.h"
#include "../debroglie/qWave.h"

void loaderFlatTics(qAvatar* avatar) {
	qSpace *space = avatar->space;
	int nPoints = space->nPoints;
//	float *posBuffer = avatar->attachViewBuffer(0, 2, 2 * nPoints);
//	float *colorBuffer = avatar->attachViewBuffer(1, 3, 2 * nPoints);
}
